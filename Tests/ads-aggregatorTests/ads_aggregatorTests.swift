    import XCTest
    @testable import ads_aggregator

    final class ads_aggregatorTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(ads_aggregator().text, "Hello, World!")
        }
    }
